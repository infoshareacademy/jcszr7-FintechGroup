@using FinTechGroup.Domain;
@using (Html.BeginForm())

@model IEnumerable<Company>
@{
    ViewData["Title"] = "IndexForGuests";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container p-3">
@*    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Companies List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Company" asp-action="Create" class="btn btn-primary">
                <i class="big bi-plus-circle"></i> &nbsp; Add New Company
            </a>
        </div> 
    </div>*@
    <br /><br />

    <table class="table" style="width:100%">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                <th>
                    Ticker
                </th>
                <th>
                    7 Days
                </th>
                <th>
                    30 Days
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td width="25%">
                        @obj.CompanyName
                    </td>
                    <td width="25%">
                        @obj.Ticker
                    </td>
                    <td width="25%">
                        @obj.SevenDaysAvg
                    </td>
                    <td width="25%">
                        @obj.ThirtyDaysAvg
                    </td>
                   @* <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Company" asp-action="Edit" asp-route-id="@obj.Id"
                           class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i>Edit</a>
                            <a asp-controller="Company" asp-action="Delete" asp-route-id="@obj.Id"
                           class="btn btn-danger mx-2"> <i class="bi bi-trash"></i>Delete</a>                           
                        </div>
                    </td>       *@            
                </tr>
            }
        </tbody>
    </table>
</div>

