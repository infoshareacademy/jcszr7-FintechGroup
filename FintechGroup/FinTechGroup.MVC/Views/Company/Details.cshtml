@using FinTechGroup.Domain;

    @model IEnumerable<ExchangeRate>
@{
        ViewData["Title"] = "Details";
        Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container p-3">
        <div class="row pt-4">
    <div class="col-6">
    <h2 class="text-primary">Exchange Rates</h2>
    </div>
    <div class="col-6 text-end">
    <a asp-controller="Company" asp-action="AddDetails" class="btn btn-primary">
    <i class="big bi-plus-circle"></i> &nbsp; Add
    </a>
    </div>
    </div>
    <br /><br />

    <table class="table" style="width:100%">
        <thead>
            <tr>
                <th>
                    StartPrice
                </th>
                <th>
                    EndPrice
                </th>
                <th>
                    EffectiveDate
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td width="25%">
                        @obj.StartPrice
                    </td>
                    <td width="25%">
                        @obj.EndPrice
                    </td>
                    <td width="25%">
                        @obj.EffectiveDate
                    </td>
                    
                    @* <td>
                <div class="w-75 btn-group" role="group">
                <a asp-controller="Company" asp-action="Edit" asp-route-id="@obj.Id"
                class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i>Edit</a>
                <a asp-controller="Company" asp-action="Delete" asp-route-id="@obj.Id"
                class="btn btn-danger mx-2"> <i class="bi bi-trash"></i>Delete</a>
                </div>
                </td>       *@
                </tr>

            }
        </tbody>
    </table>
    <br />
    <div style="position: fixed; bottom: 100px; left">
    <a asp-controller="Company" asp-action="Index" class="btn btn-secondary" style="width:150px">
        Back to List
    </a>
    </div>
</div>

